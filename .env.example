# =============================================================================
# AI Memory Service - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# See docs/02-SETUP-GUIDE.md for detailed setup instructions
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# MongoDB Atlas Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get connection string from: MongoDB Atlas → Connect → Connect your application
# Format: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
#
# IMPORTANT: 
# - Replace <username> and <password> with actual credentials
# - URL-encode special characters in password (! → %21, @ → %40, # → %23)
# - Ensure IP whitelist includes your IP or 0.0.0.0/0 for testing
# -----------------------------------------------------------------------------
MONGODB_URI=mongodb+srv://your_username:your_password@cluster.mongodb.net/?retryWrites=true&w=majority

# -----------------------------------------------------------------------------
# AWS Bedrock Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# IAM User (Permanent Credentials) - RECOMMENDED
# Get from: AWS Console → IAM → Users → Security credentials → Create access key
# DO NOT include AWS_SESSION_TOKEN when using IAM user
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=AKIA...your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# AWS SSO (Temporary Credentials) - For Enterprise
# Run: python3 scripts/refresh_aws_credentials.py
# INCLUDE AWS_SESSION_TOKEN when using SSO (credentials expire after 1-12 hours)
# -----------------------------------------------------------------------------
# AWS_SESSION_TOKEN=IQoJ...your_session_token

# -----------------------------------------------------------------------------
# AWS Region (REQUIRED)
# -----------------------------------------------------------------------------
AWS_REGION=us-east-1

# -----------------------------------------------------------------------------
# AWS Bedrock Model Configuration (OPTIONAL - Has Defaults)
# -----------------------------------------------------------------------------
# LLM Model for importance assessment and summarization
LLM_MODEL_ID=arn:aws:bedrock:us-east-1:979559056307:inference-profile/global.anthropic.claude-sonnet-4-5-20250929-v1:0

# Embedding Model for vector generation (1536 dimensions)
EMBEDDING_MODEL_ID=amazon.titan-embed-text-v1

# -----------------------------------------------------------------------------
# Service Configuration (OPTIONAL - Has Defaults)
# -----------------------------------------------------------------------------
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8182
DEBUG=false

# -----------------------------------------------------------------------------
# CORS Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://yourdomain.com

# =============================================================================
# Setup Checklist
# =============================================================================
# [ ] Copy to .env: cp .env.example .env
# [ ] Add MongoDB Atlas URI
# [ ] Add AWS IAM user credentials
# [ ] Enable Bedrock models in AWS Console
# [ ] Create MongoDB Search Indexes (docs/03-MONGODB-ATLAS.md)
# [ ] Test: python3 scripts/check_aws_credentials.py
# =============================================================================
